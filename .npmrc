# NPM Registry Configuration - TEMPORARILY USING NPM ONLY
registry=https://registry.npmjs.org/

# Access configuration for scoped packages - TEMPORARILY DISABLED
# @hive-academy:registry=http://localhost:4873/
access=public

# Authentication token (will be set in CI/CD environment)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Publishing configuration - LOCAL TESTING MODE for @hive-academy packages
@hive-academy:publish-config.access=public
@hive-academy:publish-config.registry=http://localhost:4873/

# Package configuration
save-exact=true
package-lock=true

# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=false
cache-max=86400000
