# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Access configuration for scoped packages
@hive-academy:registry=https://registry.npmjs.org/
access=public

# Authentication token (will be set in CI/CD environment)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Publishing configuration
publish-config.access=public
publish-config.registry=https://registry.npmjs.org/

# Package configuration
save-exact=true
package-lock=true

# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=false
cache-max=86400000
