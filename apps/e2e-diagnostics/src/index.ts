/**
 * @hive-academy/e2e-diagnostics
 *
 * Internal Nx application — not a publishable library.
 * Hosts the E2E diagnostic test suite for TASK_2025_063.
 *
 * Entry point: `npx nx test e2e-diagnostics` (or `npm run e2e`).
 * Probes, contracts, and reporters live under `src/`.
 */
export const APP_ID = 'e2e-diagnostics' as const;
