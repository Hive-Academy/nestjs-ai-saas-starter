export const STREAMING_CONSTANTS = {
  DEFAULT_BUFFER_SIZE: 50,
  DEFAULT_FLUSH_INTERVAL: 200,
  MAX_BUFFER_SIZE: 1000,
};

// Re-export StreamEventType from core library to maintain API compatibility
export { StreamEventType } from '@hive-academy/langgraph-core';
